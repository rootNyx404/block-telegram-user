# block-telegram-user
 A secure Telegram bot that allows channel administrators to instantly ban and unban members using simple commands like `/ban` and `/unban`. It restricts access to pre-approved admins only, helping to efficiently moderate channels and enforce rules by targeting users via their username or ID.
